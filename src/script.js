import { ShaderArt } from 'https://cdn.skypack.dev/shader-art';
import { UniformPlugin } from 'https://cdn.skypack.dev/@shader-art/plugin-uniform';

const uniforms = () => new UniformPlugin(); // not needed for now.

ShaderArt.register([]);
